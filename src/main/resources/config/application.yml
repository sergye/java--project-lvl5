server:
  port: ${PORT:5000}

base-url: /api

spring:
  application:
    name: taskManager

  mvc:
    hiddenmethod:
      filter:
        enabled: true

  liquibase:
    enabled: true
    change-log: classpath:db/changelog/changelog-master.xml